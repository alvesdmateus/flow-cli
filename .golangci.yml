version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    # Default linters
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    # Security
    - gosec
    # Style
    - gofmt
    - goimports
    # Bugs
    - bodyclose
    - noctx
    # Code quality
    - unconvert

  settings:
    errcheck:
      check-type-assertions: true
      check-blank: false

    govet:
      enable-all: true
      disable:
        - fieldalignment
        - shadow

    gosec:
      excludes:
        - G104
        - G204
        - G301
        - G302
        - G304
        - G306

issues:
  max-issues-per-linter: 50
  max-same-issues: 5
